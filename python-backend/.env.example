# .env.example - 환경 변수 설정 예시
# 실제 사용 시 .env로 복사하고 값을 수정하세요

# =================================
# 데이터베이스 설정
# =================================
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=plun

# =================================
# STT (Whisper) 설정
# =================================
# 모델 크기: tiny, base, small, medium, large
WHISPER_MODEL=medium
# 디바이스: cpu 또는 cuda
WHISPER_DEVICE=cpu
# CPU 스레드 수 (기본값: 자동 감지)
CPU_THREADS=4

# =================================
# AI 요약 설정
# =================================
# AI 제공자: openai 또는 ollama
AI_PROVIDER=ollama

# OpenAI 설정 (AI_PROVIDER=openai인 경우)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Ollama 설정 (AI_PROVIDER=ollama인 경우)
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:3b-instruct-q4_K_M

# =================================
# 서버 설정
# =================================
# 파일 업로드 제한 (바이트)
MAX_UPLOAD_BYTES=10485760  # 10MB
# 로그 레벨
LOG_LEVEL=INFO

# =================================
# 추가 설정 (선택사항)
# =================================
# 오디오 저장 기본 경로 (상대 경로)
# AUDIO_DIR=audio_chunks